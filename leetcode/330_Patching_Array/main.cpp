#include <vector>
#include <iostream>
using namespace std;

class Solution {
public:
    int minPatches(vector<int>& nums, int n) {
        set<int> result;
        for(int i = 0; i < nums.size(); i++) {
            for()
        }
        return 0;
    }
}

int main() {
    vector<int> nums = {1, 3};
    Solution s;
    cout << s.minPatches(nums, 6) << endl;
}
